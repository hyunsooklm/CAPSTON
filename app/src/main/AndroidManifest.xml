<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.our_capstone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"/>

        <activity android:name=".Main2Activity"/>

        <activity android:name=".SignInActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".SignUpActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".RoomActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".RoomInfoSettingActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".StartActivity"> <!-- 첫 화면을 이걸로 지정한다!!! -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>