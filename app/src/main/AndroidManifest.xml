<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.our_capstone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera2" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />


    <application
        android:name=".GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/our_main"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/our_main_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:hardwareAccelerated="false"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity"/>

        <activity android:name=".GlobalApplication">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".SignInActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".SignUpActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".RoomActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".RoomInfoSettingActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".OurActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".MenuActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".AlbumActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".AlbumDetailActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".AlbumInfoSettingActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".AlbumPicsActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".QnaActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".QnaDetailActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".ChatActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->
        </activity>

        <activity android:name=".SetPromiseActivity">
        </activity>
        <activity android:name=".SetPromiseActivity">
        </activity>
        <activity android:name=".PromiseDetailActivity">
        </activity>
        <activity android:name=".MemberActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->
        </activity>

        <activity android:name=".RulletActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->
        </activity>

        <activity android:name=".ConceptShotActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".LateCheckActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".MapActivity">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".PopupInviteActivity"  android:theme="@android:style/Theme.Dialog">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".PopupPicsActivity"  android:theme="@android:style/Theme.Dialog">               <!--새 엑티비티 추가할때마다 이렇게 추가해주기!!!-->

        </activity>
        <activity android:name=".StartActivity"> <!-- 첫 화면을 이걸로 지정한다!!! -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_geo_API_KEY"/>
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>